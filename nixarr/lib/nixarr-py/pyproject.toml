[project]
name = "nixarr"
version = "0.1.0"
description = "Python clients and utilities for working with Nixarr services"

dependencies = [
  "lidarr",
  "prowlarr",
  "radarr",
  "readarr",
  "sonarr",
  "whisparr",
]

[build-system]
requires = ["setuptools"]
